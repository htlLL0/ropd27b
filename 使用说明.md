# 使用说明：单张 B200 生成 9,600 条数据

本说明对应 `OPCD_Qwen38_27B_B200_9600_20260910` 文件夹的最新版。可将本地完整文件夹复制到 B200 机器，也可从 GitHub 克隆后仅补入模型权重。**所有任务日志和结果都保存在这个文件夹的 `output/` 下。**

## 1. 这个包做什么

使用本地 **Qwen3.8-27B 基础模型**，对固定的 9,600 条输入生成完整轨迹，保存原始响应、实际生成的 token、思考内容和最终答案。本地完整包已含权重；GitHub 仓库已含全部输入数据、模型配套文件和代码，只需另外补入 18 个权重分片。运行不需要 API Key。

| 配置 | 固定值 |
|---|---|
| GPU | 单张完整、空闲的 NVIDIA B200 |
| 精度与并行 | BF16，TP=1，总并发 16 |
| 温度 | 1.0 |
| 采样 | top_p=1.0，top_k 关闭（后端值 -1） |
| 思考模式 | 开启 thinking |
| 每条生成上限 | 16,384 tokens，包含思考与最终答案 |
| 上下文上限 | 32,768 tokens |
| 样本总数 | 9,600，包含最先验证的 12 条 |

输入、顺序、攻击位置、提示格式和每条 seed 都已固定。基础模型 revision、配置与来源见 [README](README_先看这里.md) 和 [运行配置](config/generation.json)。

## 2. 目标机器需要什么环境

**优先使用 Docker 方式。** 宿主机需要：

- Linux x86_64，Python 3.10 或更新。
- 一张完整且空闲的 B200。
- NVIDIA 驱动，建议 580 或更新，以预检结果为准。
- Docker 和已配置的 NVIDIA Container Toolkit，当前账号能够运行 Docker GPU 容器。
- 能容纳约 55.84 GB 数据包的磁盘空间，并为镜像、结果与缓存留出额外空间。

宿主机无需单独安装 PyTorch、Transformers 或 CUDA Toolkit。固定镜像包含 Python 3.12、CUDA 13.0、PyTorch 2.11.0+cu130、vLLM 0.24.0 和 Transformers 5.12.1。**镜像不在本文件夹里，需要首次下载，或提前在目标机导入。**

如果机器没有 Docker，请使用第 7 节的原生方式。

## 3. 首次启动：按顺序执行

如果从 GitHub 获取，在终端执行以下命令，并将 `/path/to/Qwen3.8-27B` 替换为同一 revision 的实际模型目录：

```bash
git clone https://github.com/htlLL0/ropd27b.git OPCD_Qwen38_27B_B200_9600_20260910
cd OPCD_Qwen38_27B_B200_9600_20260910
cp /path/to/Qwen3.8-27B/model-*-of-*.safetensors model/
python3 scripts/b200_collection.py verify
```

必须使用 `Qwen/Qwen3.8-27B` 的 revision `1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0`，共 18 个权重分片。输入数据、tokenizer、模型配置、权重索引和验证记录已经随 Git 提供，不需要另找数据包或重新抽样。完整校验会检查每个静态文件的大小和 SHA256。

如果已经复制了本地完整文件夹，则无需克隆或补入权重，直接进入目录（假设当前目录是它的上一级）：

```bash
cd "OPCD_Qwen38_27B_B200_9600_20260910"
chmod +x docker_b200.sh run_b200.sh install_native.sh
```

查看 GPU 和 Docker 是否可用：

```bash
nvidia-smi
docker info
```

下载固定镜像并检查 GPU。这里 `--gpu 0` 表示使用编号为 0 的卡，可替换为实际 B200 编号或 UUID：

```bash
./docker_b200.sh pull
./docker_b200.sh preflight --gpu 0
```

预检无报错，且输出包含 `bf16_probe: pass` 对应的 JSON 字段后，启动后台任务：

```bash
./docker_b200.sh start --gpu 0
```

启动后先查看一次状态：

```bash
./docker_b200.sh status
```

后台容器会依次执行：文件与模型哈希校验 → 9,600 条输入分词与参数校验 → 模型加载 → 12 条真实生成验证 → 剩余 9,588 条生成 → 完整结果审计与汇总 → 退出模型服务。

12 条验证通过后会自动进入全量。`start` 返回 `launched_preflight_pending` 只表示后台任务已创建，实际阶段以 `status` 为准。Docker 后台任务不依赖当前 SSH 连接。

## 4. 查看进度和日志

持续查看进度，每 5 秒刷新，时间按北京时间显示：

```bash
watch -t -n 5 ./docker_b200.sh status
```

没有 `watch` 时，直接重复执行 `./docker_b200.sh status` 即可。

| 显示的阶段 | 含义 |
|---|---|
| `preflight` | 正在检查文件、环境与输入；尚未提交生成请求 |
| `model_loading` | 加载模型 |
| `smoke` | 验证最先的 12 条样本 |
| `bulk` | 全量生成中 |
| `draining` | 停止新增请求，等待在途请求保存完毕 |
| `paused` | 已暂停 |
| `generation_complete` | 已完成全量生成及最终审计 |
| `failed` | 有异常，需要查看日志和错误记录 |

ETA 在本次运行完成至少 100 条后开始显示，会随实际生成长度变化。启动时哈希校验与加载模型可能需要一些时间。

查看主程序日志：

```bash
./docker_b200.sh logs
```

查看模型服务日志：

```bash
tail -n 100 -F output/main/server.log
```

在这些查看命令中按 `Ctrl+C` 只退出查看，不会暂停后台任务。Docker 主程序日志直接保存为 `output/main/supervisor.log`，容器退出后仍然保留。

## 5. 日志和结果保存在哪里

```text
output/
├── logs/                    # 启动、预检、检查、暂停、镜像下载等命令日志
├── setup/                   # 原生安装日志和 pip 缓存（使用原生安装时）
└── main/                    # 默认任务目录
    ├── supervisor.log       # 主程序日志、标准错误及异常堆栈
    ├── server.log           # vLLM 模型加载和推理日志
    ├── progress.json        # 实时进度
    ├── completed.jsonl      # 已完成样本的索引记录，不是完整轨迹汇总
    ├── cases/
    │   └── 00000/
    │       ├── execution.json
    │       ├── request.json
    │       ├── raw_response.json
    │       └── trajectory.json
    ├── generations.jsonl    # 全部 9600 条完成后的完整轨迹汇总
    ├── generation_summary.json
    ├── completion.json      # 完成回执
    ├── cache/               # 运行缓存
    └── tmp/                 # 运行临时文件
```

逐条结果会持续保存到 `cases/`。`generations.jsonl` 仅在全部生成完成并通过最终审计后写出；运行中还没有这个文件是正常现象。

任务目录还会记录配置、模型与输入校验结果、硬件、环境、暂停或失败等 JSON 回执。遇到异常的 case 会额外保存 `error.json`。

**交付或备份时，复制整个 `output/` 文件夹即可带走任务日志和结果。** 如需一致的中途备份，先按第 6 节暂停并确认在途请求已经清空。

## 6. 暂停、恢复与另开任务

请求暂停：

```bash
./docker_b200.sh pause
./docker_b200.sh status
```

脚本停止提交新请求，等待已有请求保存，然后退出模型服务。长思考请求可能使暂停需要等待；单请求超时仍为 3,600 秒。

等待状态显示 `paused`、在途数为 0，且状态输出中的容器 `Running` 为 `false` 后，才恢复同一任务：

```bash
./docker_b200.sh start --gpu 0 --resume
```

恢复会校验并跳过已完成记录。如果存在未完成或带错误的 case，脚本会拒绝自动重新生成；先查看保存的请求和错误记录。任务不会自行重试或恢复，也不会覆盖已有结果。

如需另开独立任务，可以使用新的 run ID：

```bash
./docker_b200.sh start --gpu 0 --run-id run2
./docker_b200.sh status --run-id run2
./docker_b200.sh logs --run-id run2
./docker_b200.sh pause --run-id run2
```

这时结果位于 `output/run2/`。启动、查看、暂停和恢复都要使用同一个 run ID，并选择空闲的 B200。

## 7. 没有 Docker：原生运行方式

建议使用 Python 3.12，并确保它带有 `venv` 模块。NVIDIA 驱动和 B200 要求与前面一致。先安装固定依赖，再启动：

```bash
./install_native.sh
./run_b200.sh preflight --gpu 0
./run_b200.sh start --gpu 0
```

安装脚本会在包目录建立 `.venv/`，安装日志位于 `output/setup/install_native.log`。模型已经在包里；安装 Python 依赖需要联网。

查看、暂停和恢复：

```bash
./run_b200.sh status
watch -t -n 5 ./run_b200.sh status
tail -n 100 -F output/main/supervisor.log

./run_b200.sh pause
# 确认 paused、在途数为 0 后再执行：
./run_b200.sh start --gpu 0 --resume
```

原生方式与 Docker 使用相同的 `output/` 布局。一个 run ID 应始终使用同一种运行方式管理。

如果平台会在 SSH 退出时清理用户进程，请在平台的持久任务环境或 `tmux` 中前台运行 `./run_b200.sh run --gpu 0`。已安装相同依赖的环境也可以通过 `OPCD_PYTHON=/已有环境/bin/python` 指定。

## 8. 如何确认全部完成

以下三项同时满足：

1. `status` 显示 `generation_complete`，完成数为 `9600/9600`。
2. `output/main/completion.json` 中 `status` 为 `complete`、`samples` 为 `9600`、`server_released` 为 `true`。
3. `output/main/generations.jsonl` 存在，含 9,600 行完整轨迹。

查看完成回执和行数：

```bash
cat output/main/completion.json
wc -l output/main/generations.jsonl
```

如果用了其他 run ID，将命令中的 `main` 替换为对应名称。

此包只生成训练候选轨迹，不会启动训练。输出中的 `training_ready=false` 是预期状态：后续还需要 A/U 标注、到达长度上限样本的处理、T+ 模型前向验证、可靠性校准及训练集成。

## 9. 常见问题

| 现象 | 查看或处理方式 |
|---|---|
| Docker 命令不可用或权限不足 | 检查 Docker 服务及当前账号权限；也可使用原生方式 |
| 无法识别 `nvidia` runtime 或 GPU | 检查 NVIDIA Container Toolkit 是否已为 Docker 配置 |
| `B200 required` | 所选设备不是 B200，核对 `nvidia-smi` 中的设备及编号 |
| `Selected B200 is already in use` | 选择一张空闲卡；脚本不会停止现有任务 |
| `Environment differs from pinned image` | 使用包中固定的 Docker 镜像，或通过安装脚本建立匹配的原生环境 |
| `SHA256 mismatch` | 文件缺失、损坏或版本混用；重新同步对应版本的文件与 `bundle_manifest.json`，保留已有输出 |
| `FileExistsError` 或已有任务提示 | 检查现有状态；干净暂停后用 `--resume`，独立新任务用新的 `--run-id` |
| 主程序日志还没创建 | 先看 `output/logs/docker_start.log` 或 `output/logs/native_start.log` |
| 长时间处于 `model_loading` | 查看 `output/main/server.log` 中的加载进度及报错 |
| 显示 `failed` | 查看 `supervisor.log`、`server.log`、`failure.json` 及出错 case 的 `error.json` |
| 提示未完成 case，无法恢复 | 保留 `output/`，先核对该 case 已保存的请求、响应和错误；不要删除记录来绕过检查 |

脚本、配置、数据与 `bundle_manifest.json` 应来自同一版数据包。当前版本的正式输出目录是 `output/`。

本包已完成本地文件、输入和适配代码校验，以及日志落盘测试；**尚未在真实 B200 上实跑**。目标机器上的预检与最先 12 条真实生成验证仍是正式启动的一部分。
